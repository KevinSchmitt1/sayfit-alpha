# SayFit Alpha – Requirements
# ============================

# --- Core pipeline ---
openai>=1.10.0              # OpenAI-compatible client (used for Groq API)
python-dotenv>=1.0.0        # .env file loading

# --- Retrieval / RAG ---
faiss-cpu>=1.7.4            # FAISS vector search (CPU version)
sentence-transformers>=2.2.0 # Sentence embeddings (all-MiniLM-L6-v2)

# --- Data handling ---
pandas>=2.0.0
numpy>=1.23.0

# --- Voice input (Step 0) ---
sounddevice>=0.4.6
scipy>=1.10.0
openai-whisper>=20231117
# torch is already pulled in by sentence-transformers

# --- Recipe step (step6) ---
duckdb>=0.10.0

# --- API ---
fastapi>=0.136.0
uvicorn[standard]>=0.47.0
httpx>=0.28.0
python-multipart>=0.0.9     # required by FastAPI for UploadFile / multipart form data


# --- Observability ---
langfuse>=4.0.0,<5.0.0          # LLM tracing for step1_extraction and step3_reranker
prometheus-fastapi-instrumentator>=0.10.0
# formatting

ruff
black

# API
fastapi>=0.136.0
uvicorn[standard]>=0.47.0
httpx>=0.28.0