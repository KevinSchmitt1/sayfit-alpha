# SayFit Alpha — API container dependencies
# Excludes voice recording (sounddevice) — PortAudio must never run in the container.
# openai-whisper is included for POST /transcribe (transcription only, no mic access).
# scipy and dev tools (ruff, black) remain CLI-only.

# --- Core pipeline ---
openai>=1.10.0
python-dotenv>=1.0.0

# --- Transcription ---
openai-whisper>=20231117
python-multipart>=0.0.9  # required by FastAPI for UploadFile / multipart form data

# --- Retrieval / RAG ---
faiss-cpu>=1.7.4
sentence-transformers>=2.2.0

# --- Data handling ---
pandas>=2.0.0
numpy>=1.23.0

# --- Observability ---
langfuse>=4.0.0,<5.0.0
prometheus-fastapi-instrumentator>=0.10.0


# --- Recipe step ---
duckdb>=0.10.0

# --- API ---
fastapi>=0.136.0
uvicorn[standard]>=0.47.0
httpx>=0.28.0
